//ESLint untuk cek konsistensi code menggunakan aturan di ESLint

/**
 * Konfigurasi nya 
 * 1. Jalankan npm install --save-dev eslint
 * 2. Jalankan npx eslint --init
 * 3. Konfigurasi rules nya di eslint.config.js
 * 4. Cek konsistensi aturan code npx eslint fileName.js
 * 5. Jika Lolos tidak muncul output, jika tidak akan muncul code error problem tidak sesuai aturan estlint
 */

const nama = "Daniel";

console.log(nama);
